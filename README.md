slider-wr
=========

Slider Wr es un plugin, para wordpress de gestion de banner.

	<?php
	  /*
	  * Slider Wr Simple
	  *
	  * @param string, int $arg (wp_item.ID, wp.title).
	  * @return string.
	  *
	  **/
	  echo slider_wr($arg);
	?>
